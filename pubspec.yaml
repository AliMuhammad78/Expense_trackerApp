name: money_tracker_001
description: A money tracker app clone.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  fl_chart: ^0.69.0
  uuid: ^4.5.1
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  # Database dependencies
  sqflite: ^2.3.3+2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # If you have assets like images for your login page, 
  # make sure to uncomment and list them here later:
  # assets:
  #   - assets/images/